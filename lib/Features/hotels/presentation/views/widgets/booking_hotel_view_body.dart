import 'package:flutter/material.dart';
class BookingHotelViewBody extends StatelessWidget {
  const BookingHotelViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
